import 'package:flutter/material.dart';

class PayHubScreen extends StatelessWidget {
  const PayHubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Pay')));
  }
}
